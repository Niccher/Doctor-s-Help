pipeline {
agent none
  stages {
    stage("build & SonarQube Scanner") {
    agent any
      steps {
      withSonarQubeEnv('SonarQ_installation') {
      sh 'mvn clean package sonar:sonar'
      }
      }
    }
  }
}
